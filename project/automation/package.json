{
  "name": "@missao/mobs-automation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "bin": {
    "mobs": "./dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build --silent && node --test \"dist/test/*.test.js\"",
    "mobs": "node dist/src/cli.js"
  },
  "dependencies": {
    "@resvg/resvg-js": "2.6.2",
    "ajv": "^8.20.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^7.0.2"
  }
}
